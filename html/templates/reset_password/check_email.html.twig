{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}

<div class="card-group support mt-3 mt-lg-5">
    <div class="card ">
        <div class="card-body">
            <h1 class="h1 mb-3 font-weight-normal">Check your email</a></span></h1>

            <p> If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.<br/>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
            <p>If you don't receive an email please check your spam folder or <a href="{{ path('app_forgot_password_request') }}">try again</a>.</p>
        </div>
    </div>
</div>
{% endblock %}
